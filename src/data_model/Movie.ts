import * as Immutable from 'immutable'
import * as moment from 'moment'


export type Movie = Immutable.Map<string, any>
export type Theatre = Immutable.Map<string, any>
export type Schedule = Immutable.Map<string, any>